const express = require('express');
const database = require('../database/database');
const route = express.Router();

route.get('/:param', function(req, res) {
    //Add logic
});

route.get('/:param', function(req, res) {
    //Add logic 
});



